# Criação de um vetor de strings

vetor = sample(c("Norte", "Nordeste", "Sudeste", "Sul", "Centro-Oeste"),
               500,
               replace = TRUE)

# Criação de um vetor de fatores

vetorDeFatores <- factor(vetor)
